<div>
    <label for='en'>${label}</label>
    <input type="number decimal" name="${namen}" value="${calculatorInstance.getProperty(name)}"
           required="true" min="1.0" max="6.0" id="${name}"/>
</div>